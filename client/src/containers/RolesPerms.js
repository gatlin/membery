import React from 'react';
import { connect } from 'react-redux';
import * as RPactions from '../services/roles_perms';

export const RolesPermsPage = props => (
    <div id='roles-perms-page'>
      <p>This space intentionally left blank.</p>
    </div>
);
